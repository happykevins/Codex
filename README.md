# Codex

This repository contains small programming examples.

## Tetris Game

Run the simple curses-based Tetris game with:

```bash
python3 tetris.py
```

Use the arrow keys to move and rotate the pieces. Press `q` to quit.
